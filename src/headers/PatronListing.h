#include <string>
#include <iostream>

using namespace std;

#include "Patron.h"

PatronListing{
    private:

        Patron* head; // points to the first patron in the list

        PatronList(){
            head = null;
        }

    public:
        void 
    
    
    
    
        /*void addPatron(string name, int libraryNumber){
        Patron* newPatron = new Patron(name, librayNumber);
        newPatron -> next=head;
        head = newPatron;
        cout<<"New Patron Registered Sucessfully ! \n";

    */
    }
    /*
    void deletePatron (string libraryNumber){
        Patron * temp = head;
        
        if (temp && temp-> libraryNumber == librayNumber)
            head=-> next;
            delete temp;
            cout <<"Deleted Patron!";
            return;
            }
            
        if (!temp){
            cout<<"Patron not found \n";
            }
            
            */
    void 
}